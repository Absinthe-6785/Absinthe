# K-31.3 — Archive Audit

**Branch:** `k31-release-candidate`  
**Entry:** Analytics tab → `ArchiveShell`

---

## Mode Switching

| Mode | Component | Tab wired | Status |
| ---- | --------- | --------- | ------ |
| Home | `ArchiveHomeView` | ✅ default | Complete |
| Period | `ArchiveBranchView` mode=period | ✅ | Actionable branch |
| Area | `ArchiveBranchView` mode=area | ✅ | Area pills + open note |
| Timeline | `ArchiveBranchView` mode=timeline | ✅ | Milestones + trace CTA |

`ArchiveModeSwitcher` provides in-app tablist (`role="tablist"`) — no URL sync (known deferred item).

---

## Home Surface

| Section | Navigation | Status |
| ------- | ---------- | ------ |
| Mark calendar | Day click → `openTraceDayNavigation` | ✅ |
| Mark calendar | Month label → `openArchiveMarkMonthNavigation` | ✅ |
| Recent milestones | Entry click → `openNote(noteId)` | ✅ |
| Area pills | Pill click → `openNote(areaNoteId)` | ✅ |
| Browse links | → `openArchiveBrowseDestination` | ✅ |
| Empty CTA | → Notes tab | ✅ |

Tests: `archiveShell.test.ts`, `analyticsViewArchiveLanding.test.ts`, component tests per section.

---

## Branch Surfaces

### Period

- Period browse links from projection
- **Open current period** → `openTraceRangeNavigation` (or Notes fallback)
- Mark calendar reused from Home projection

### Area

- Area pills with `onAreaClick` → open area note
- Empty state when no marked areas

### Timeline

- Recent milestones list
- **Open timeline range** → trace navigation via browse destination
- Mark calendar for context

---

## Dead Ends Identified

| Issue | Severity | Notes |
| ----- | -------- | ----- |
| No URL deep-link to Period/Area/Timeline | P2 | Refresh returns Home; documented deferred |
| `ArchivePlaceholderView` legacy | P3 | Still exists for tests; not default path |
| Browse custom period | P2 | Opens trace; depends on vault content |

**No P0 dead-end interactions** found in code paths — all primary buttons invoke navigation helpers.

---

## Localization

UI section copy: English when `language: en` (K-31.2).  
Projection data (month labels, untitled notes): locale via `useArchiveHomeProjection(now, language)`.

---

## Consolidation Notes

Main had K-31 Pass 1–5 squash-merged (#189). K-31.1/K-31.2 merged into RC without conflicts. Archive branch views (`ArchiveBranchView`) present on RC after merge.
