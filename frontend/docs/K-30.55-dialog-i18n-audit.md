# K-30.55 — Dialog i18n Audit

**Branch:** `k30-localization-search-polish`

---

## Audited Dialogs

| Dialog | File | Status |
| ------ | ---- | ------ |
| Create / Edit Event | `EventNoteDialog.tsx` | **Migrated** — `event*` keys |
| Milestone note | `MilestoneNoteDialog.tsx` | **Migrated** — `milestone*` keys |
| Create Project | `CreateProjectDialog.tsx` | K-30.54 — `project*` keys |
| Create Milestone | `CreateMilestoneDialog.tsx` | K-30.54 — `milestone*` keys |
| Workspace Search | `WorkspaceSearchPalette.tsx` | K-30.55 — search + filter keys |

## Coverage

- Buttons: `save`, `close`, `cancel` (shared) + dialog-specific actions
- Placeholders: `eventTitlePlaceholder`, `milestoneDefaultTitle`, …
- Validation: thrown errors surfaced; custom range uses `traceInvalidRange`
- Helper text: optional field labels (`eventTimeOptional`, `milestoneLabelOptional`)

## Gaps (low priority)

- Learning-path onboarding dialogs — English-only labels if opened from maps (unchanged this milestone)
- Native `<input type="date">` chrome is OS-locale dependent

## Success Criteria

**Met:** Event, milestone, project creation, and workspace search dialogs are language-consistent across en/ko/ja.
