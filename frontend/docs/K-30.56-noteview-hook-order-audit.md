# K-30.56 — NoteView Hook-Order Audit

**Branch:** `k30-tablet-accessibility-polish`

---

## TDZ Issues Found & Fixed

| Hook | Line (before) | Dependency | Declared at (before) | Minified name risk |
| ---- | ------------- | ---------- | -------------------- | ------------------ |
| `handleCreateReadingNote` | 839 | `noteUpdate` | 1059 | — |
| `handleUpdateProjectDescription` | 959 | `noteUpdate` | 1059 | — |
| `handleUpdateProjectStatus` | 965 | `noteUpdate` | 1059 | — |
| `handleUpdateMilestoneStatus` | 978 | `noteUpdate` | 1059 | — |
| `handleUpdateMilestoneTargetDate` | 992 | `noteUpdate` | 1059 | — |
| `handleUpdateNoteProperties` | 1001 | `noteUpdate` | 1059 | — |

**Previously fixed:** `activeNote` used in deps at line 834 before declaration at 1118 (production minified as `q`).

**Not present:** `milestoneEditorData`, `learningPathData`, `researchDashboard`, `studyDashboard` — no such symbols; closest are `projectEditorData`, `unifiedWorkspaceDashboard`, `learningPathOverview`, `learningPath`.

---

## Dependency Graph (summary)

```
Store selectors (notes, activeNoteId, updateNote, …)
  └─ activeNote (useMemo)
  └─ noteUpdate (useCallback) ← moved early
  └─ useNoteWorkspace → handleLeaveDashboardForNote, applyWorkspaceToNotes, …
       └─ trace projections (useMemo)
       └─ formulaQueryCatalog, knowledgeQueryInfo, visibleNotes (useMemo) ← moved up
       └─ productivity callbacks (useCallback) — depend on activeNote, noteUpdate, openCreatedNote
  └─ active-note panel memos (toc, projectEditorData, learningPath, …)
  └─ editor/chrome callbacks + effects
  └─ render constants (c, CSS, folderLabel)
```

**Rule enforced:** no `useCallback`/`useMemo` dependency array references a `const` declared later in the component.

---

## Reorganization

| Section | Contents |
| ------- | -------- |
| A | Store selectors |
| B | Derived state (`activeNote`, trace, search/list memos, panel memos) |
| C | `noteUpdate` helper |
| D | Callbacks |
| E | Layout constants + effects |
| F | Render |

---

## Verification

Automated dep scan: **0 TDZ issues**  
`npm run typecheck` · `npm run build` · `npm run test` (1722/1722)
