export {
  getProperty,
  listProperties,
  listUserProperties,
  normalizePropertyKey,
  parseNoteMarkdown,
  removeProperty,
  serializeNoteMarkdown,
  setProperty,
} from './noteProperties';

export { normalizeNoteProperties } from '../../../noteUtils';
