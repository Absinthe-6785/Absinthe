export {
  getProperty,
  listProperties,
  normalizePropertyKey,
  parseNoteMarkdown,
  removeProperty,
  serializeNoteMarkdown,
  setProperty,
} from './noteProperties';

export { normalizeNoteProperties } from '../../../noteUtils';
