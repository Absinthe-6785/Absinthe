/**
 * K-30.16 — Archive Home is the default Analytics tab landing surface.
 *
 * Retained for rollback safety: set to `false` to restore legacy Analytics
 * widgets without removing Archive Home code. Legacy AnalyticsView body remains
 * in place and is skipped when this flag is true.
 */
export const ARCHIVE_SHELL_ENABLED = true;
